package static2;

public class DecoUtil2 {

    // 클래스 메서드, 정적메서드(static)
    public static String deco(String str) {
        String result = "*" + str + "*"; // return "*" + str + "*"; 이렇게 해도 된다.
        return result;
    }
}
